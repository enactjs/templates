{
  "name": "@enact/templates",
  "version": "4.5.5",
  "description": "A collection of Enact-based templates.",
  "private": true,
  "author": "Jason Robitaille <jason.robitaille@lge.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/enactjs/templates.git"
  },
  "license": "Apache-2.0",
  "scripts": {
    "bootstrap": "lerna exec -- npm --loglevel error --no-package-lock install",
    "publish": "lerna publish",
    "publish-next": "lerna publish --npm-tag=next",
    "clean": "lerna clean --yes",
    "lerna": "lerna",
    "lint": "eslint ."
  },
  "eslintConfig": {
    "extends": "enact-proxy"
  },
  "eslintIgnore": [
    "node_modules"
  ],
  "devDependencies": {
    "eslint-config-enact-proxy": "^1.0.7",
    "lerna": "^7.4.2"
  }
}
